[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "blacknode-runtime"
version = "0.4.2"
description = "Authenticated remote deployment runtime for Blacknode."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["blacknode_runtime*"]
